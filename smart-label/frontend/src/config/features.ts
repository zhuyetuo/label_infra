/**
 * 先关掉、但代码留着的功能。
 *
 * 为什么不直接删：这些都是花了功夫做出来、并且**实测过**的东西。删掉之后
 * 下次想再试就得从头写一遍，连当初为什么不行都得重新量一遍。留着开关，
 * 结论写在 README 里，想再验就改一个字。
 */

/**
 * 夜视增强（播放器上的「弱/中/强」+ 片段行上的「夜视」按钮）。
 *
 * **2026-09-22 关掉。** 实测狗场夜里那几路单间，原片只用到 14~40 这 26 级
 * 亮度（满量程 256），铺满要放大 9.81×，30 帧堆栈把信噪比抬 ×5.5——传感器
 * 本来就没记录下更多信息，判不出"在不在抓挠"。深度学习那条（Retinexformer +
 * SMID）更糟：两种输入都在编，输出成块的橙绿紫。
 *
 * 详见 imu_train 的 README「夜间画面：实测到头了，算法救不动」。
 * 真正的解法是给狗场单间补红外补光，不是接着调算法。
 *
 * 想再打开：把下面改成 true，重新部署（`deploy/deploy_all.sh`）；算法机那边
 * 还要在 vision_service/.env 里写 LOWLIGHT_ENABLED=1 再 `./up.sh deploy -g`。
 */
export const NIGHT_VISION = false;
